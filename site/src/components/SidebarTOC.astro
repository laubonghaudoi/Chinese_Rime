---
import { branchSchemaCount, getBranchesByNameInitial } from "../data/loader";

const branches = getBranchesByNameInitial();
---
<aside class="toc" aria-label="語系目錄">
  {branches.map((branch) => (
    <a
      href={`#${branch.key}`}
      data-toc-branch={branch.key}
      data-empty={branch.status === "missing" ? "true" : "false"}
    >
      <span>{branch.name}<code>{branch.iso_639_3 || "—"}</code></span>
      <b data-toc-count={branchSchemaCount(branch)}>{branchSchemaCount(branch)}</b>
    </a>
  ))}
</aside>

<style>
  .toc {
    position: sticky;
    top: 96px;
    align-self: start;
    display: grid;
    gap: 0;
  }
  a {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 8px;
    padding: 4px 0;
    text-decoration: none;
    font-family: var(--ff-sans-tc);
    font-size: 14.5px;
    line-height: 1.2;
  }
  a:hover {
    font-weight: 700;
  }
  span {
    display: flex;
    align-items: baseline;
    gap: 6px;
    min-width: 0;
  }
  code {
    color: var(--zhu);
    font-family: var(--ff-mono);
    font-size: 1em;
  }
  b {
    font-family: var(--ff-sans);
    font-weight: 600;
    font-variant-numeric: tabular-nums;
  }
  [data-empty="true"] {
    color: var(--ink-meta-4);
  }
  @media (max-width: 1024px) {
    .toc {
      display: none;
    }
  }
</style>
